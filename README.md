to Run the Project :
	g++ -std=c++17 scripts.cpp -o script -I/usr/include/poppler/cpp -L/usr/lib -lpoppler-cpp
then : use spliter to split data ./spliter
then use ./search "keyword"  exemple -> ORACLE
